# Pinned for Python 3.11 (local env + CI + Streamlit Cloud must all match).
# Unpinned (>=) ranges let Cloud resolve to Python 3.14 + pandas 3 + numpy 2.5 +
# pyarrow 25, whose native stack segfaults after boot. These are the exact
# versions verified working (install + browser e2e boot) on Python 3.11.
# NOTE: this file does NOT pin the interpreter — also set the Streamlit Cloud
# app's Python version to 3.11 (Advanced settings), or the segfault can persist.
streamlit==1.59.1
plotly==6.9.0
yfinance==1.5.1
pandas==3.0.3
numpy==2.4.6
requests==2.33.1
joblib==1.5.3
beautifulsoup4==4.15.0
openai>=1.0.0
